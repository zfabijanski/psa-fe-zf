import styled from "styled-components";

export const PruLabel = styled.h4`
  font-weight: 700;
  font-size: 20px;
  font-stretch: normal;
  line-height: 28px;
  color: ${({ theme }) => theme.newColors.neutral100Default};
  margin-bottom: 32px;
`;
